apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: apps-http-echo
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`echo.svc.lab.local`)
      kind: Rule
      services:
        - name: apps-http-echo
          port: 80
