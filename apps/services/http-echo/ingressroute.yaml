apiVersion: networking.k8s.io/v1
kind: IngressRoute
metadata:
  name: http-echo
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`echo.svc.lab.local`)
      kind: Rule
      services:
        - name: http-echo
          port: 80
