apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: kubernetes-dashboard
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`dashboard.svc.lab.local`)
      kind: Rule
      services:
        - name: kubernetes-dashboard-kubernetes-dashboard-web  
          port: 8000
