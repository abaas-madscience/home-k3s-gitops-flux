apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: flux-capacitor
  namespace: flux-capacitor
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`flux.svc.lab.local`)
      kind: Rule
      services:
        - name: flux-capacitor
          port: 80
