apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: longhorn-ui
  namespace: infa-longhorn
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`longhorn.svc.lab.local`)
      kind: Rule
      services:
        - name: longhorn-frontend
          port: 80
