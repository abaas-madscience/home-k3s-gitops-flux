apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: kube-db-test
  namespace: flux-system
spec:
  interval: 1m
  path: ./apps/services/kubernetes-dashboard
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  targetNamespace: kube-db-test
  wait: true
